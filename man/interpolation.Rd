% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator-interpolation.R
\name{interpolation}
\alias{interpolation}
\title{Interpolate spectral data to target wavelengths}
\usage{
interpolation(spectra, target_wavelengths, method = "linear")
}
\arguments{
\item{spectra}{A ruffs_spectra object}

\item{target_wavelengths}{Numeric vector of target wavelengths (in nm)}

\item{method}{Interpolation method. Options:
\describe{
\item{"linear"}{Linear interpolation (default)}
\item{"spline"}{Cubic spline interpolation}
\item{"constant"}{Nearest-neighbor (constant) interpolation}
}}
}
\value{
A ruffs_spectra object with interpolated data
}
\description{
Interpolates spectral data to a new set of wavelengths.
Handles irregular spacing and non-monotonic sequences in the original data.
}
\examples{
\dontrun{
# Read SVC data
spec <- svcreader("sample.sig")

# Interpolate to 1nm spacing
spec_interp <- interpolation(spec, seq(400, 2400, 1))

# Use spline interpolation
spec_spline <- interpolation(spec, seq(400, 2400, 1), method = "spline")
}
}
