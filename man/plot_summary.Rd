% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator-summarize.R
\name{plot_summary}
\alias{plot_summary}
\title{Plot Spectral Summary with Error Bands}
\usage{
plot_summary(
  summary,
  groups = NULL,
  error_type = c("se", "sd", "none"),
  colours = NULL,
  alpha = 0.2,
  legend_position = "topright",
  xlim = NULL,
  ylim = NULL,
  ...
)
}
\arguments{
\item{summary}{A \code{ruffs_spectra_summary} object created by
\code{\link[=summarise_spectra]{summarise_spectra()}}.}

\item{groups}{Character vector of group names to plot. If \code{NULL} (default),
all groups are plotted.}

\item{error_type}{Character string specifying the error band type:
\code{"se"} (default), \code{"sd"}, or \code{"none"}.}

\item{colours}{Character vector of colours for each group. If \code{NULL},
uses default colour palette.}

\item{alpha}{Numeric value (0-1) for error band transparency. Default is 0.2.}

\item{legend_position}{Position for legend. Options include \code{"topright"},
\code{"topleft"}, \code{"bottomright"}, \code{"bottomleft"}, or \code{"none"} to suppress.}

\item{xlim}{Numeric vector of length 2 specifying x-axis (wavelength) limits.
If \code{NULL} (default), uses full wavelength range.}

\item{ylim}{Numeric vector of length 2 specifying y-axis limits.
If \code{NULL} (default), automatically calculated to fit data and error bands.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
Invisibly returns the summary object.
}
\description{
Creates a plot of mean spectra with optional error bands (SD or SE) for
one or more groups from a spectral summary.
}
\examples{
\dontrun{
summary <- summarise_spectra(grouped_spectra)

# Plot all groups with SE bands
plot_summary(summary)

# Plot specific groups with SD bands
plot_summary(summary,
  groups = c("site1_oak", "site2_oak"),
  error_type = "sd")

# Plot without error bands
plot_summary(summary, error_type = "none")

# Specify axis limits
plot_summary(summary, xlim = c(400, 900), ylim = c(0, 0.5))

# Focus on red edge region
plot_summary(summary, xlim = c(680, 750), ylim = c(0.1, 0.6))

# Full SWIR range
plot_summary(summary, xlim = c(1500, 2400))
}

}
